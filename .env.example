# Archivo de ejemplo .env (no lo subas al repo)
# Ruta al modelo local ggml/gguf (opcional). Ej: C:\models\gpt4all-quant.bin
MODEL_PATH=

# VOSK STT model path (opcional, para reconocimiento de voz offline)
VOSK_MODEL_PATH=

# SMTP para envío de correos (si quieres habilitar)
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=you@example.com
SMTP_PASS=yourpassword

# Confirmar acciones: true/false (por defecto false en CLI, puedes forzarlo con --confirm-actions)
CONFIRM_ACTIONS=false
